/** Значение интервала главного цикла по умолцанию (30 кадров в секунду) */
export const defaltTimerValue30Frames: number = 1000 / 30;
/** Значение интервала главного цикла по умолцанию (60 кадров в секунду) */
export const defaltTimerValue60Frames: number = 1000 / 60;